apply from: '../build.gradle'

dependencies {
    implementation project(':internal/logging')
    implementation project(':internal/runtime_shared')
}

uploadArchives {
    repositories {
        mavenDeployer {
            pom.project {
                name = 'DSLink SDK'
                artifactId = 'dslink'
                description = 'SDK for the IoT DSA protocol'
            }
        }
    }
}
