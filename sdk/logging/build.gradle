apply from: '../build.gradle'

uploadArchives {
    repositories {
        mavenDeployer {
            pom.project {
                name = 'DSLink SDK'
                artifactId = 'logging'
                description = 'Lightweight SLF4J binding'
            }
        }
    }
}
